{
  "name": "part3-a-backend",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../4. Back-end/2. Node.js + Express/FullStackOpen/part3/notes-app-frontend && npm run build && cp -r dist ../../../../../Deploy/notes-app",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Matias Martearena",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "4.19.2"
  },
  "devDependencies": {
    "standard": "^17.1.0"
  },
  "eslintConfig": {
    "extends": "standard"
  }
}